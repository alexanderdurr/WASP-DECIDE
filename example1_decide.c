// to-do
// make an example that works on the global variables and can read the 
/*

    #include <stdio.h>
    #include <decide.h>

    int main(void){
        // Set up a the problem in example 1 in the document
        // ...

        // run decide which changes the global variables
        // LAUNCH, CMV, PUM, FUV
        decide();

        printf("as expected")
        return 0;
    }

*/